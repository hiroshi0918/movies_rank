<div class="content-row">
  <div class="container">
    <%= form_with(model: @movie, local: true) do |form| %>
      <%= render 'layouts/error_messages', model: form.object %>
      <h3>映画投稿の編集</h3>
      <%= form.label :title, "映画タイトル", class: "visually-hidden" %>
      <%= form.text_field :title, placeholder: "映画タイトル", class: "container--name" %>
      <%= form.label :original_title, "原題（任意）", class: "visually-hidden" %>
      <%= form.text_field :original_title, placeholder: "原題（任意）", class: "container--name" %>
      <%= form.label :director, "監督名", class: "visually-hidden" %>
      <%= form.text_field :director, placeholder: "監督名", class: "container--director" %>
      <%= form.label :category, "ジャンル", class: "visually-hidden" %>
      <%= form.text_field :category, placeholder: "ジャンル", class: "container--category", list: "movie-categories" %>
      <%= form.label :youtube_url, "YouTubeのURL", class: "visually-hidden" %>
      <%= form.text_field :youtube_url, placeholder: "YouTubeのURL", class: "container--youtube" %>
      <%= form.label :detail, "映画詳細", class: "visually-hidden" %>
      <%= form.text_area :detail, placeholder: "映画詳細", rows: "10", class: "container--detail" %>
      <datalist id="movie-categories">
        <% (@categories || []).each do |category| %>
          <option value="<%= category %>"></option>
        <% end %>
      </datalist>
      <%= form.label :image, class: 'container--image' do %>
        <span>画像を変更</span>
        <%= form.file_field :image, class: 'hidden' %>
      <% end %>

      <%= form.submit "更新する", class: "container--btn" %>
    <% end %>
  </div>
</div>
