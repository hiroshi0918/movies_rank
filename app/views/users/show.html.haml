.movieTitle
  = @user.nickname
  さんの投稿一覧
.content3
  - @movies.each do |movie|
    .content3__left
      .content3__left--image
    .content3__right
      .content3__right__top
        = link_to movie.title, movie_path(movie.id), class: "content3__right__top--title"
      -# .content3__right__bottom
      -#   .content3__right__bottom--userName
      -#     = link_to movie.user.nickname, user_path(movie.user), class: "content3.content3__right__bottom--userName--btn"
.LikedMovies
  %h3いいねしている投稿 
  - @user.liked_movies.each do |movie|
    = movie.director
    = movie.title
    %br